# Session Handoff - 2026-04-13

## 项目

独立 SaaS Rust 后端, git repo: `git@github.com:jeryaiwei/saas.git` (branch: main)
工作目录: `/Users/jason/Documents/Project/node/tea-saas/server-rs`

## 当前状态

- 192 端点 / 31 模块 / 326 tests / 0 failures / clippy clean
- 12 smoke scripts / 119 steps / all green
- Swagger UI: /swagger-ui (Bearer JWT, 中文 tag/summary)
- Observability v1.0 已落地 (root span + field inheritance)
- Operlog v1.1: route-level OperlogLayer + Extension<PgPool>
- Repo executor: PgExecutor / Acquire / Transaction 三层规范
- 租户架构: 三层(超级→平台→一般), 套餐过滤, 切换快照恢复
- Mail Send: lettre SMTP + AES-256-CBC 密码解密 (对齐 NestJS CryptoHelper)
- SMS Send: 阿里云真实接入 (HMAC-SHA1 签名), 腾讯/华为 mock
- 异步发送: tokio::spawn + Semaphore 背压 (mail=10, sms=20)
- File Upload: local/OSS/COS 三存储 + 客户端直传 + MD5 秒传
- RedisExt trait: set_ex/get_json/exists/del 强类型封装

## 模块清单

```text
src/
├── auth/           4 endpoints (login, logout, code, info)
│                   + 2 public (tenant/list, refresh-token)
├── system/         12 modules, 84 endpoints
│   ├── config 7     ├── menu 9       ├── tenant 9 (+switch/select/clear/status)
│   ├── dept 7       ├── post 6       ├── tenant_package 6
│   ├── dict 12      ├── role 11      ├── tenant_dashboard 6
│   ├── audit_log 3  ├── user 15 (+profile/pwd/dept-tree)
│   └── file_manager 19  upload 4 (server+download+client auth+callback)
│
├── message/        11 modules, 55 endpoints
│   ├── notice 5           ├── mail_account 6    ├── sms_channel 6
│   ├── notify_template 6  ├── mail_template 5   ├── sms_template 5
│   ├── notify_message 11  ├── mail_log 2        ├── sms_log 2
│   ├── mail_send 4        └── sms_send 3
│
├── monitor/        4 modules, 13 endpoints
│   ├── oper_log 4    ├── online_user 2
│   ├── login_log 3   └── server_info 1
│   └── cache 7 (Redis 监控)
│
└── domain/         28 repos
```

## 本轮完成

### 第一批: Mail Send + SMS Send (7 endpoints)

1. Mail Send 模块 (4 endpoints: send/batch/resend/test)
2. SMS Send 模块 (3 endpoints: send/batch/resend)
3. 共享模板解析器 (${key} substitution, 15 unit tests)
4. SMTP infra (lettre + spawn_blocking + 30s timeout)
5. SMS client 抽象 (trait + factory + 3 mock)
6. tokio::spawn + Semaphore 背压 (mail=10, sms=20)
7. Log repo 扩展 (insert + update_status)
8. Template repo 扩展 (find_enabled_by_code)
9. 7 新错误码 (7170-7192) + i18n 全覆盖
10. AppError::business_with_msg 变体
11. Fix flaky notice test + clippy type_complexity

### 第二批: 真实服务接入

1. AES-256-CBC 加解密 (scrypt log_n=14 对齐 NestJS, NestJS 密文交叉测试)
2. Mail 密码解密 (build_smtp_params 自动 decrypt, 失败回退明文)
3. MAIL_PASSWORD_KEY 从 AppConfig 读取 (MailConfig + default.yaml)
4. 阿里云 SMS 真实接入 (HMAC-SHA1 签名, RFC 3986 编码, dysmsapi.aliyuncs.com)
5. reqwest 全局池化 (LazyLock, 10s timeout, 5s connect)

### 第三批: Code Review 修复 (2 轮)

1. SmtpParams / SysMailAccount / SysSmsChannel Debug 脱敏
2. SMS retry 退避对齐 (2s/4s/8s)
3. tracing 覆盖异步 send 成功/失败路径
4. update_status 错误不再 .ok() 吞掉
5. batch SMS client Arc 复用 (不再 per-mobile 创建)
6. batch mail scrypt 一次 (循环外 build_smtp_params)
7. sms_log send_time 成功时更新
8. batch email ValidateEmail 校验
9. Mailbox from_name 引号包裹
10. execute_with_retry 消除 unreachable!()

## 新增依赖

| crate | 用途 |
| --- | --- |
| lettre | SMTP 邮件发送 |
| aes + cbc + scrypt | AES-256-CBC 加解密 (NestJS compat) |
| hex | 十六进制编解码 |
| hmac + sha1 | 阿里云 HMAC-SHA1 签名 |
| reqwest | SMS HTTP API 调用 |
| base64 | 签名 base64 编码 |

## 框架规范 (docs/framework/) — 8 份

| 规范 | 版本 | 状态 |
| --- | --- | --- |
| pagination-spec | v1.0+v1.1 | 已落地 |
| error-envelope-spec | v1.0 | 已落地 |
| observability-spec | v1.0 | 已落地 |
| repo-executor-spec | v1.0 (含 Acquire) | 已落地 |
| pagination-indexes | v1.0 | 已落地 |
| openapi-spec | v1.0 | 已落地 |
| operlog-spec | v1.1 | 已落地 |
| tenant-spec | v1.0 | 已落地 |

## 下一步

1. **Vue Web 灰度切换** — 188 端点覆盖管理端，改 proxy target 即可
2. **腾讯/华为 SMS 真实接入** — 替换 mock client
3. **文档同步** — README 端点数/模块数更新

## 用户偏好

- 中文回复
- 不需要手动 git (push 到 github 需要确认)
- 新 repo 单查询用 `impl PgExecutor<'_>`
- 多读查询用 `impl Acquire<'_>`
- 多写操作用 `&mut Transaction`
- 批量删除循环必须在事务内
- Service 2+ 写操作必须包事务

## 新会话开场

```text
项目: /Users/jason/Documents/Project/node/tea-saas/server-rs
读 docs/RESUME-2026-04-13.md, 继续下一步任务。
```
