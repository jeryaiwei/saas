# Session Handoff - 2026-04-13

## 项目

独立 SaaS Rust 后端, git repo: `git@github.com:jeryaiwei/saas.git` (branch: main)
工作目录: `/Users/jason/Documents/Project/node/tea-saas/server-rs`

## 当前状态

- 181 端点 / 28 模块 / 304 tests / 0 failures / clippy clean
- 12 smoke scripts / 119 steps / all green
- Swagger UI: /swagger-ui (Bearer JWT, 中文 tag/summary)
- Observability v1.0 已落地 (root span + field inheritance)
- Operlog v1.1: route-level OperlogLayer + Extension<PgPool>
- Repo executor: PgExecutor / Acquire / Transaction 三层规范
- 租户架构: 三层(超级→平台→一般), 套餐过滤, 切换快照恢复

## 模块清单

```text
src/
├── auth/           4 endpoints (login, logout, code, info)
│                   + 2 public (tenant/list, refresh-token)
├── system/         12 modules, 84 endpoints
│   ├── config 7     ├── menu 9       ├── tenant 9 (+switch/select/clear/status)
│   ├── dept 7       ├── post 6       ├── tenant_package 6
│   ├── dict 12      ├── role 11      ├── tenant_dashboard 6
│   ├── audit_log 3  ├── user 15 (+profile/pwd/dept-tree)
│   └── file_manager 19
│
├── message/        9 modules, 48 endpoints
│   ├── notice 5           ├── mail_account 6    ├── sms_channel 6
│   ├── notify_template 6  ├── mail_template 5   ├── sms_template 5
│   ├── notify_message 11  ├── mail_log 2        └── sms_log 2
│
├── monitor/        4 modules, 13 endpoints
│   ├── oper_log 4    ├── online_user 2
│   ├── login_log 3   └── server_info 1
│   └── cache 7 (Redis 监控)
│
└── domain/         28 repos
```

## 今日完成

1. Post + Config + Dict 模块 (25 endpoints, 26 tests)
2. Observability v1.0 (root span, auth field propagation, middleware instrument)
3. Swagger UI 集成 (utoipa + utoipa-axum + OpenApiRouter)
4. OpenApiRouter 重构 (path 只写一次, 净删 328 行)
5. 中文 tag + summary 全覆盖
6. Operlog 框架 (route-level OperlogLayer + Extension<PgPool>)
7. 所有 43→65 写路由加 operlog
8. Notice + OperLog + Logininfor 模块 (12 endpoints, 11 tests)
9. Repo executor: find_page 全部迁移到 Acquire
10. Notify Template + Message 模块 (17 endpoints)
11. Mail Account + Template + Log 模块 (13 endpoints)
12. SMS Channel + Template + Log 模块 (13 endpoints)
13. 模块迁移: message/ + monitor/ 顶级目录
14. Auth 补齐 (tenant/list, refresh-token)
15. Tenant 切换 (select-list, switch, clear, status) + switchedFrom 快照
16. User profile (get/update/change-pwd/dept-tree)
17. Audit Log (3 endpoints)
18. Monitor: online-user + server-info + cache
19. Tenant Dashboard (6 endpoints, tokio::try_join 并行)
20. File Manager (19 endpoints: 文件夹/文件/分享/回收站/版本/存储)
21. 租户架构加固:
    - 登录: 超级管理员路径 + tenantId 指定 + 租户状态/过期校验
    - 切换: DB binding 判断身份(不用 session.tenantId) + 完整校验 + switchedFrom
    - 创建: 三层约束 + 套餐必填
    - sys_code 从套餐获取
22. 用户安全加固:
    - 删除/停用/角色变更 → bump_user_token_version 注销 token
    - 删除时清理 sys_user_role 绑定
    - platform_id 参数化 (不再硬编码 '000000')
    - 权限 SQL: LEFT JOIN → INNER JOIN 防御
23. 常量统一: SUPER_TENANT_ID + PLATFORM_ID_DEFAULT
24. 7 个新 smoke scripts (post/config/dict/notice/notify/operlog/loginlog)
25. 8 份框架规范 (+ openapi + operlog + tenant)
26. NestJS 修复: 过期校验 + 层级约束 + 绑定清理 + token 注销 + isPlatformAdmin

## 框架规范 (docs/framework/) — 8 份

| 规范 | 版本 | 状态 |
| --- | --- | --- |
| pagination-spec | v1.0+v1.1 | 已落地 |
| error-envelope-spec | v1.0 | 已落地 |
| observability-spec | v1.0 | 已落地 |
| repo-executor-spec | v1.0 (含 Acquire) | 已落地 |
| pagination-indexes | v1.0 | 已落地 |
| openapi-spec | v1.0 | 已落地 |
| operlog-spec | v1.1 | 已落地 |
| tenant-spec | v1.0 | 已落地 |

## 下一步

1. **Vue Web 灰度切换** — 181 端点覆盖管理端，改 proxy target 即可
2. **Mail Send + SMS Send** (7 endpoints) — 需对接外部服务
3. **NestJS 修复提交** — 6 处已修改待 commit
4. **文档同步** — README 端点数/模块数更新

## 用户偏好

- 中文回复
- 不需要手动 git (push 到 github 需要确认)
- 新 repo 单查询用 `impl PgExecutor<'_>`
- 多读查询用 `impl Acquire<'_>`
- 多写操作用 `&mut Transaction`
- 批量删除循环必须在事务内
- Service 2+ 写操作必须包事务

## 新会话开场

```text
项目: /Users/jason/Documents/Project/node/tea-saas/server-rs
读 docs/RESUME-2026-04-13.md, 继续下一步任务。
```
